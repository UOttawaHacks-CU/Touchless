document.addEventListener("DOMContentLoaded", function () {
    // This is called after the browser has loaded the web page
    document
      .getElementById("no1")
      .addEventListener("click", handleSubmitButton1);
  });
  
function handleSubmitButton1() {
    // do something when submit button is clicked
    window.location.href = "./population.html";
}

document.addEventListener("DOMContentLoaded", function () {
    // This is called after the browser has loaded the web page
    document
      .getElementById("no2")
      .addEventListener("click", handleSubmitButton2);
  });
  
function handleSubmitButton2() {
    // do something when submit button is clicked
    window.location.href = "./immunocompromised1.html";
}

document.addEventListener("DOMContentLoaded", function () {
    // This is called after the browser has loaded the web page
    document
      .getElementById("cont1")
      .addEventListener("click", handleSubmitButton3);
  });
  
function handleSubmitButton3() {
    // do something when submit button is clicked
    window.location.href = "./immunocompromised2.html";
}

document.addEventListener("DOMContentLoaded", function () {
    // This is called after the browser has loaded the web page
    document
      .getElementById("cont2")
      .addEventListener("click", handleSubmitButton4);
  });
  
function handleSubmitButton4() {
    // do something when submit button is clicked
    window.location.href = "./immunocompromised3.html";
}

document.addEventListener("DOMContentLoaded", function () {
    // This is called after the browser has loaded the web page
    document
      .getElementById("cont3")
      .addEventListener("click", handleSubmitButton5);
  });
  
function handleSubmitButton5() {
    // do something when submit button is clicked
    window.location.href = "selfisolate.html";
}

document.addEventListener("DOMContentLoaded", function () {
    // This is called after the browser has loaded the web page
    document
      .getElementById("no3")
      .addEventListener("click", handleSubmitButton6);
  });
  
function handleSubmitButton6() {
    // do something when submit button is clicked
    window.location.href = "./testingpositive.html";
}

document.addEventListener("DOMContentLoaded", function () {
    // This is called after the browser has loaded the web page
    document
      .getElementById("no4")
      .addEventListener("click", handleSubmitButton7);
  });
  
function handleSubmitButton7() {
    // do something when submit button is clicked
    window.location.href = "./finish.html";
}